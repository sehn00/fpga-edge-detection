# fpga-edge-detection
FPGA-based streaming image processing pipeline that receives RGB pixel data over UART, converts it to grayscale, applies Sobel edge detection, and transmits the processed edge output back through UART.
